# PPSeg RKNN Models

Place PPSeg RKNN model files here. The default configuration references:

- `pp_liteseg_128.rknn`

Model weights are not committed by default. Document the source model, conversion command and license before distributing any compiled `.rknn` files.
