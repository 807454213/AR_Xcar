# Xcar2.md 全面同步设计

## 目标

将 `Xcar2.md` 同步到 2026-07-11 的实际工程状态，并补充区域赛现场比赛手册 V3.0 对项目策略的约束。只修改文档，不改变程序行为或配置。

## 信息来源与优先级

1. 运行行为、状态优先级、串口模式和配置语义以当前源码为准。
2. 当前实车参数以 `configs/config.json` 为准，文档只描述键的用途，不复制可能频繁变化的数值或密钥。
3. 比赛任务、计分和合规要求以 `第21届智能车竞赛人工智能模型组比赛细则.md` V3.0 为准。

## 更新范围

- 增加 V3.0 比赛约束摘要，说明三圈和隐藏穿越面优先于金币收益。
- 修正单帧数据流为 `async-latest` AI 推理，并记录无新结果时复用上一帧检测。
- 修正完整 `DriveState` 优先级，补充 `StableSpeed` 和 `cmd02_mode=8`。
- 补充金币可达带、近处金币绕过可达性判断及不可达点白色显示。
- 说明路牌默认 LLM 主判定、本地规则兜底。
- 更新配置键、Pipeline 职责、测试命令和常见陷阱。

## 验证

- 检索文档，确保旧优先级和缺少 mode 8 的串口表不再存在。
- 对照 `drive_state.h`、`drive_control.cpp`、`Pipeline.cpp`、`config.h` 和 V3.0 规则手册复核关键描述。
- 文档修改不需要编译；通过 Markdown 内容检查和 `git diff --check` 验证。
