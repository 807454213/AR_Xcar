# Superpowers 历史设计记录

`plans/` 和 `specs/` 保存按日期形成的设计与实施记录，用于追溯当时的问题、假设和执行步骤。

这些文件不是当前运行手册，其中可能保留：

- 旧工程路径 `/home/orangepi/Desktop/Xcar2`；
- 已完成迁移前的中间状态；
- 当时有效、后来被替换的配置和测试命令；
- 计划中的代码片段，而非当前源码。

不要根据这些历史文件判断当前行为，也不要为了“同步”而批量改写历史内容。当前事实来源按优先级为：

1. 当前源码与 `configs/config.json`；
2. 根目录 `PROJECT_STATUS.md`；
3. 根目录 `Xcar2.md` 与 `README.md`；
4. `TC264.md` 和比赛细则；
5. 本目录历史记录。
