# Map Assets

Place local SLAM/map binary assets such as `sdf_map.bin` here.

Map binaries are not committed by default because they may be generated, site-specific or large. Document how to generate or obtain them for a public release.
