# Xcar2 下位机（TC264）导读（给 AI 助手）

本文档按最新下位机工程 **`/home/orangepi/Desktop/TC264`** 的源码更新，说明 TC264 在 Xcar2 系统中的职责、串口契约、状态机、控制环与调参入口。上位机工程仍在 `/home/orangepi/Desktop/Xcar`，上位机协议实现位于 `src/io/uart.cpp`、`Uart/uart.hpp` 与 `Uart/HardwareProxy.hpp`。

修改串口协议、状态机或速度/转向模式时，必须同步检查上下位机两侧并实车验证。

---

## 1. 下位机在系统中的位置

```text
Orange Pi / RK3588（Xcar 上位机）
  摄像头/AI/PPSeg/tc_process
  → 每帧 0x01 横向 error
  → 事件型 0x02/0x03/0x05/0x07/0x08/0x09/0x0B
  ← 0x01 左轮 Δtick、0x02 右轮 Δtick、0x03 yaw

UART1 460800 8N1

TC264（/home/orangepi/Desktop/TC264）
  RX ISR → 512 字节环形缓冲
  主循环 → 解析固定帧、上传 pending 数据、菜单/屏幕刷新
  1 ms PIT → UART 看门狗、IMU 融合、yaw 刷新、2 ms 控制内环
  5 ms PIT → 状态机、按键、1 s 计时、蜂鸣器节拍
```

TC264 不做视觉。它执行上位机给出的横向误差和离散模式字，回传编码器增量与 yaw。

---

## 2. 固件工程速查

| 路径 | 作用 |
|---|---|
| `user/cpu0_main.c` | CPU0 主循环、1 ms/5 ms PIT、UART1 RX ISR |
| `code/send_data.c/.h` | 固定 9 字节串口帧、CRC、RX 分发、TX 上传、环形缓冲解析 |
| `code/reliability_core.c/.h` | 可宿主测试的可靠性小核心：CRC、重同步、快照、限幅、Flash CRC |
| `code/Global.c/.h` | 全局状态、PID/速度/元素参数、UART 缓冲、常量 |
| `code/other_code.c` | `whether_change_state()`、元素 flag 超时、屏幕状态显示、蜂鸣器/低压 |
| `code/car_run.c/.h` | PRERUN/RUNNING/STOP/手动直行转向、速度模式、PWM 输出、死区 |
| `code/pid.c/.h` | 舵机 PD/模糊 PID、差速 PID、回赛道 error 放大 |
| `code/fuzzy_pid.c/.h` | `Fuzzy_Enable=1/2` 的模糊转向控制 |
| `code/encoder.c/.h` | 2 ms 编码器增量采样、里程累计、速度上限换算、速度 PI |
| `code/imu_fusion.c/.h` | ICM42688 + Madgwick，陀螺仪零偏校准，yaw 输出 |
| `code/Init.c/.h` | 外设、PWM、PID、PIT、UART1 初始化 |
| `code/Menu.c`, `Menu_flag.c` | IPS200/按键调参菜单与多套预设 |
| `code/Flash.c/.h` | 参数持久化，Flash section 21 page 4 |

---

## 3. 启动与主循环

`core0_main()` 流程：

1. `clock_init()`、`debug_init()`。
2. `Init_all()` 初始化按键、IPS200、编码器、蜂鸣器、电机 PWM、舵机 PWM、ICM42688、IMU 融合、PID、模糊 PID、PIT 和 UART1。
3. 主循环持续执行：
   - `uart1_process_ring_buffer()`：从 UART1 环形缓冲拼帧并分发。
   - `uart_upload_service()`：发送编码器/yaw pending 数据。
   - `Menu_Switch()`：本机按键菜单。
   - `showing_real_time_data()`：在运动调试页刷新状态、error、编码器、里程、yaw。
   - `low_voltage_detection()`：按 5 s 节拍执行低压保护检查。
   - `wireless >= 1` 时用默认调试串口打印左右编码器、目标速度和 `flag_stop`。

`Init_all()` 关键配置：

| 项 | 当前代码 |
|---|---|
| 电机 PWM | 四路 17 kHz |
| 舵机 PWM | 50 Hz，初值 `servo_zhongzhi` |
| 舵机 PID 输出限幅 | `PID_Init(&Servo_pid, ..., 75)` |
| 差速 PID 输出限幅 | `PID_Init(&Chasu_pid, ..., 66)` |
| PIT_1 | 1 ms |
| PIT_2 | 5 ms |
| UART1 | `460800`，TX `P33_12`，RX `P33_13`，开启 RX 中断 |

---

## 4. 定时任务

| 周期 | 位置 | 行为 |
|---|---|---|
| 1 ms | `cc60_pit_ch0_isr` | RUNNING 时累加 UART 看门狗；执行 `IMU_Fusion_Task()`；更新 `send_Yaw` |
| 10 ms | `cc60_pit_ch0_isr` | 置 `yaw_upload_pending=1` |
| 2 ms | `cc60_pit_ch0_isr` | `Encoder_Data_get()`、`cal_speed_to_encoder()`、`Control_Update_Steering()`、按 `car_state` 控制速度/舵机、`output_pwm()` |
| 5 ms | `cc60_pit_ch1_isr` | `whether_change_state()`，包含元素 flag 超时和运行状态切换 |
| 10 ms | `cc60_pit_ch1_isr` | `key_scanner()` |
| 1 s | `cc60_pit_ch1_isr` | PRERUN 调 `pre_run_countdown()`；RUNNING/STOP_TEMP 调 `running_count()` |
| 50 ms | `cc60_pit_ch1_isr` | 蜂鸣器节拍，`flag_buzzer` 最多保持约 200 ms |
| 5 s | `cc60_pit_ch1_isr` | 置 `voltage_test_flag=1`，主循环执行 `low_voltage_detection()` |

### 4.1 UART 看门狗

只有 `car_state == RUNNING` 时，1 ms 中断才累加 `uart1_watchdog_cnt`。计数达到 `666` 后置 `flag_uart_timeout=1`，状态机会把 RUNNING 切到 STOP_TEMP。

以下事件会清看门狗或超时标志：

- `process_received_data()` 收到 CRC 正确、`len=4` 且命令字在 `0x01..0x0B` 范围内的有效帧。
- 收到 `0x05` 发车命令。
- 本机菜单 `start_car()`。

UART1 RX ISR 只搬运字节到环形缓冲，不再因收到任意字节就清看门狗；噪声字节、CRC 错帧和未知命令不会维持 RUNNING。当前行为是 **约 666 ms 未收到有效 UART 控制帧则临时停车**，不是旧文档里的 1500 ms。

---

## 5. 串口帧格式

| 字节 | 字段 | 说明 |
|---|---|---|
| 0 | `0x55` | 帧头 |
| 1 | `cmd` | 命令字 |
| 2 | `len` | 正常固定为 `4` |
| 3..6 | payload | 小端；`float`、`int16` 或 `uint8`，短类型高字节补 0 |
| 7..8 | CRC16 | 高字节在前 |

常量：

```c
#define param1_longest_byte  4
#define param1_send_all_byte 9
```

CRC 使用 **CRC16-CCITT**，多项式 `0x1021`，初值 `0xFFFF`，覆盖 `[0..len+2]`。发送端固定按 4 字节 payload 计算 CRC，也就是覆盖前 7 字节。

当前解析器从环形缓冲中按 `param1_send_all_byte == 9` 收满一帧再处理。`process_received_data()` 会拒绝非 `len=4` 帧；遇到坏帧后会在已收数据中寻找下一个 `0x55` 并重同步，减少单字节丢失导致后续整串报文错位的概率。

UART1 RX ISR 只把硬件 FIFO 搬到 512 字节环形缓冲。帧同步、CRC、看门狗喂狗和命令分发都在主循环执行；环形缓冲满时新字节会被丢弃并累计 `uart1_rx_overflow_count`。

---

## 6. 命令字方向对照

同一 `cmd` 在上下行含义不同，联调时最容易看错：

| cmd | 上位机 → TC264 | TC264 → 上位机 |
|---|---|---|
| `0x01` | 横向 `error`，`float` | 左轮 Δtick，`int16` |
| `0x02` | 运动模式 `flag_stop`，`uint8` | 右轮 Δtick，`int16` |
| `0x03` | 保护 `flag_protect`，`uint8` | yaw，`float`，0..360 度 |
| `0x04` | 蜂鸣器 `flag_buzzer`，`uint8` | `cmd_send_to_up`，`uint8`，发送函数支持但当前上传服务不主动发 |
| `0x05` | 发车 `flag_start_car`，`uint8` | 当前不上传 |
| `0x06` | 手动运行 `flag_key_run`，`uint8` | 当前不上传 |
| `0x07` | 直/弯标志 `flag_curve`，`uint8` | 当前不上传 |
| `0x08` | 最大速度 `max_speed`，`float` | 当前不上传 |
| `0x09` | 元素/DriveState `flag_element`，`uint8` | 当前不上传 |
| `0x0A` | 已删除，不再由上位机下发圈数；下位机自行记圈 | 当前不上传 |
| `0x0B` | 分岔 `flag_fork`，`uint8` | 当前不上传 |

`uart_upload_service()` 当前只自动上传：

- 编码器 pending：依次 `send(0x01)`、`send(0x02)`。
- yaw pending：`send(0x03)`。

---

## 7. 上位机 → TC264 命令语义

### 7.1 `0x01` error

- 类型：`float`。
- 作用：更新全局 `error`，2 ms 舵机控制直接使用。
- 符号：沿用上位机契约，正 error 表示目标在画面中心右侧，固件 `PID_Calc_Servo(0, error)` 输出对应舵机偏置。
- 特殊值：`feedback == 160` 时舵机输出固定 `-75`；`feedback == -160` 时固定 `75`，用于边界丢失兜底。
- `flag_stop==7` 时，舵机前先执行 `error * return_track_error_multiplier` 并限幅到 `[-160, 160]`。

### 7.2 `0x02` flag_stop

`flag_stop` 是运动模式，不只是停车标志。RUNNING 下的基础目标速度由 `running_control()` 选择，随后叠加差速 `pidoutput_chasu`。

| value | 上位机名称 | 固件基础速度 |
|---|---|---|
| `0` | NORMAL | `Motor_PWM`；若 `flag_element==3/4`，改用车元素速度 |
| `1` | STOP | RUNNING → STOP_TEMP；目标速度 0，舵机继续跟 error |
| `2` | FAST | `fast_ps_pwm`，未被 `max_speed` 限制时使用 `Speed_Kp_fast/Ki_fast` |
| `3` | SLOW | `slow_ps_pwm` |
| `4` | GOLD_SLOW | `slow_coin_pwm`，启用金币差速参数 |
| `5` | RETURN_TRACK | `slow_boundary_pwm`，启用边界/回线类差速参数 |
| `6` | GOLD_BAND | `slow_coin_side_pwm` |
| `7` | FAST_BACK / 回赛道 | `coin_return_road_pwm`，舵机 error 可乘 `return_track_error_multiplier` |
| `8` | STABLE_SPEED / very fast | `very_fast_pwm`，使用 `S8_PD_Mode` 或 `flag_stop==8` 专用模糊 PID 表 |

所有基础目标速度都会经过 `clamp_target_speed()`，最终不超过 `max_speed_encoder * 0.9`。

### 7.3 `0x03` flag_protect

| value | 行为 |
|---|---|
| `0` | 解除保护 |
| `1` | 锁存 STOPPED，清 `flag_start_car` 和 `flag_key_run` |

`flag_protect==1` 优先级高于临时停车和普通运行。保护锁存后，必须收到 `0x03=0` 才能再次通过 `0x05` 发车。

### 7.4 `0x04` flag_buzzer

收到后写 `flag_buzzer`。若本机参数 `buzzer >= 1` 且 `flag_buzzer==1`，50 ms 节拍控制蜂鸣器；`count_buzzer >= 4` 后自动清 `flag_buzzer`，约 200 ms。

### 7.5 `0x05` flag_start_car

收到后写 `flag_start_car`，并清 UART 看门狗和超时标志。

状态机条件：

```text
car_state == STOPPED && flag_start_car == 1 && flag_protect == 0
  → init_imu()
  → PRERUN
  → 清 count_pre / flag_stop / count_buzzer / flag_key_run
```

当前 `pre_run_countdown()` 会等待 IMU 校准完成并满足 `pre_run_time=1` 的秒级节拍后进入 RUNNING。IMU 校准在 6 s 超时分支中至少要求 `IMU_MIN_CAL_SAMPLES` 个静止样本；样本不足时会保持 PRERUN，屏幕提示 `imu keep still` 并继续等待下一轮校准窗口。

推荐上位机发车序列仍是：

```text
0x03 = 0  → 清保护
0x02 = 0  → 清 STOP
0x05 = 1  → 发车
```

### 7.6 `0x06` flag_key_run

用于本机/串口手动调试：

| value | 行为 |
|---|---|
| `0` | 若当前是手动直行/左转/右转，切 STOPPED |
| `1` | RUN_STRAIGHT，舵机中值，速度 `key_Motor_PWM` |
| `2` | TURN_LEFT；连续发送会把 `turn_angle` 每次加 25，最大 75 |
| `3` | TURN_RIGHT；连续发送会把 `turn_angle` 每次加 25，最大 75 |

状态机会把已消费的 `flag_key_run` 改成 `88`，防止重复触发。

### 7.7 `0x07` flag_curve

直/弯标志。当前主要影响：

- `flag_stop==8` 且 `S8_PD_Mode==1` 时，选择 `Servo_Kp_s8_z/Kd_s8_z` 或 `Servo_Kp_s8_w/Kd_s8_w`。
- `flag_stop==8` 且使用模糊 PID 时，选择直道/弯道专用加速表。

普通模糊 PID 路径在当前代码中传入 `isCurve=0`，不使用 `flag_curve`。

### 7.8 `0x08` max_speed

- `max_speed > 12.0f`：`max_speed_encoder = 2000`，等价于解除速度上限。
- `0 < max_speed <= 12.0f`：按轮径、编码器分辨率和减速比换算为编码器目标上限。
- 换算后 `clamp_target_speed()` 只允许实际目标速度达到 `max_speed_encoder * 0.9`。
- 若换算结果小于 1，则强制为 1。

### 7.9 `0x09` flag_element / DriveState

上位机把顶层 `DriveState` 值作为 `0x09` payload 下发，有效值通常是 1..9，`Launch=10` 不下发。

固件行为：

- 收到非 0 值：写 `flag_element`，并设置 `element_timeout_ticks = 400`。
- 收到 0：写 `flag_element=0`，并清超时。
- `whether_change_state()` 每 5 ms 调一次 `update_element_timeout()`；400 tick 约 2 s，超时后自动清 `flag_element`。
- 当前只有 `flag_stop==0 && flag_element==3` 和 `flag_stop==0 && flag_element==4` 有特殊控制分支，分别对应上位机 `AvoidCar` 与 `LeavingCar`。其他 DriveState 值主要用于记录/显示/后续扩展。

`flag_element==3/4` 特殊分支包括：

- 基础速度改为 `car_pwm_close` / `car_pwm_leave`。
- 舵机 PID 使用 `Car_PD_Mode`，可选普通 PD 或模糊 PID。
- 差速 PID 使用车元素 close/leave 专用参数。

### 7.10 `0x0A` / `0x0B`

- `0x0A`：上位机已删除该通道，不下发圈数；圈数由下位机自行维护。
- `0x0B`：写 `flag_fork`，当前固件未在主控制分支中使用。

---

## 8. TC264 → 上位机上传

| cmd | 类型 | 来源 | 触发 |
|---|---|---|---|
| `0x01` | `int16` | `encoder_upload_left_snapshot` | 2 ms 编码器采样快照置 pending |
| `0x02` | `int16` | `encoder_upload_right_snapshot` | 与左轮同一采样周期快照 |
| `0x03` | `float` | `yaw_upload_snapshot` | 每 10 ms yaw 快照置 pending |

编码器上传是 **本周期增量 tick**，不是绝对位置，也不是 m/s。右轮在采样时取反：

```c
motor_left.encoder_speed  = encoder_get_count(TIM6_ENCODER);
motor_right.encoder_speed = -encoder_get_count(TIM5_ENCODER);
encoder_clear_count(...);
```

上传前会先捕获左右编码器与 yaw 快照。编码器左右轮来自同一个 2 ms 采样周期，避免左/右帧跨周期混用；如果上一组编码器数据尚未由主循环发出，新的 2 ms 采样到来会增加 `encoder_upload_drops`，然后覆盖 pending 快照。

---

## 9. Carstate 状态机

```text
STOPPED --(0x05=1 && protect=0)--> PRERUN --(IMU 校准完成且 1 s 预跑到期)--> RUNNING
   ^                                  |                              |
   |                                  | protect=1                    |
   |                                  v                              |
   +---------------------------- STOPPED <---------------------------+
                                      ^
                                      |
RUNNING --(flag_stop=1 或 UART 超时)--> STOP_TEMP
STOP_TEMP --(flag_stop!=1 且 UART 恢复)--> RUNNING

手动调试：
flag_key_run=1/2/3 → RUN_STRAIGHT / TURN_LEFT / TURN_RIGHT
flag_key_run=0 且处于手动态 → STOPPED
```

| 状态 | 控制输出 |
|---|---|
| `STOPPED` | `stop_control()`，电机 PWM 为 0，舵机回中 |
| `PRERUN` | `stop_control()`；等待 IMU 校准完成并满足 `pre_run_time=1` |
| `RUNNING` | `running_control()`，舵机跟 error，速度按 `flag_stop/flag_element` |
| `STOP_TEMP` | `stop_temp_control()`，目标速度 0，舵机继续跟 error |
| `RUN_STRAIGHT` | 舵机中值，速度 `key_Motor_PWM` |
| `TURN_LEFT` | 舵机 `servo_zhongzhi + turn_angle`，速度 `key_Motor_PWM` |
| `TURN_RIGHT` | 舵机 `servo_zhongzhi - turn_angle`，速度 `key_Motor_PWM` |

`running_count()` 在 RUNNING 和 STOP_TEMP 中都会每秒累加。达到 `runtime` 秒后，清 `flag_start_car` 并切 STOPPED。`runtime` 默认源码值是 32，但菜单预设通常会写成 108；实际以 Flash/菜单当前值为准。

---

## 10. 转向、差速与速度控制

### 10.1 转向

2 ms 内环执行：

```text
Control_Update_Steering()
  → PID_Calc_Servo(&Servo_pid, 0, effective_error)
  → pidoutput
  → servo_pwm = servo_zhongzhi + pidoutput
```

模式选择：

| 条件 | 转向参数来源 |
|---|---|
| `flag_stop==8` | `S8_PD_Mode`；可用普通 PD 的 `Servo_Kp_s8_z/w`，或 `flag_stop==8` 专用模糊表 |
| `flag_stop==0 && flag_element==3` | `Car_PD_Mode`；车辆接近专用 `Servo_Kp_car_close/Kd` 或模糊 PID |
| `flag_stop==0 && flag_element==4` | `Car_PD_Mode`；车辆离开专用 `Servo_Kp_car_leave/Kd` 或模糊 PID |
| `Fuzzy_Enable==1` | 普通 `FuzzyPID_Calc()`，D 项来自 error rate |
| `Fuzzy_Enable==2` | `FuzzyPID_Calc_Gyro()`，D 项来自 `filtered_gyro.z` |
| 其他 | 普通 `Servo_Kp/Servo_Kd` PD |

`ERROR_RATE_CLAMP` 当前为 35，用于限制 error 差分阶跃。

### 10.2 差速

`PID_Calc_Chasu()` 只在以下模式输出非零差速：

- `flag_stop==4`：金币减速差速参数。
- `flag_stop==2`：行人/快速模式差速参数。
- `flag_stop==5`：边界/RETURN_TRACK 类差速参数。
- `flag_stop==7`：回赛道差速参数。
- `flag_stop==0 && flag_element==3/4`：车辆接近/离开专用差速参数。

`Chasu_D_Mode==0` 时 D 项用 error rate；`Chasu_D_Mode==1` 时 D 项用 `filtered_gyro.z`。差速输出叠加方式：

```c
motor_left.target_speed  -= pidoutput_chasu;
motor_right.target_speed += pidoutput_chasu;
```

### 10.3 速度 PI 与 PWM

速度环是增量式 PI：

```text
output = Kp * (当前速度误差 - 上次速度误差) + Ki * 当前速度误差
motor.duty += output
```

关键限制：

- PI 单步输出限幅到 `[-6000, 6000]`。
- duty 限幅到 `[-9999, 9999]`，并带 anti-windup。
- `apply_motor_deadzone()` 对小 PWM 和电机死区做处理；左右电机死区默认都是 500。
- `target_speed==0` 且编码器绝对值小于 3 时，最终 PWM 直接归零。

---

## 11. 编码器、里程与速度上限

硬件：

- 左编码器：`TIM6_ENCODER`，`P20_3/P20_0`。
- 右编码器：`TIM5_ENCODER`，`P10_3/P10_1`，采样后取反。

里程累计：

- 轮径：`0.063185 m`。
- 编码器分辨率：`1024`。
- 减速比：`68/30`。
- `real_dist = 平均累计 tick * METERS_PER_TICK * 1.23069`。

速度上限换算：

```text
max_speed_encoder =
  max_speed * 2 * 1024 * (68/30) / 1000 / 0.063185 / pi / 1.06678
```

当 `max_speed > 12.0` 时，直接设置 `max_speed_encoder=2000`。

---

## 12. IMU 与 yaw

`Init_all()` 会初始化 ICM42688 与 `IMU_Fusion_Init()`。发车或菜单 `init_imu` 会执行：

```text
IMU_Fusion_Init()
IMU_Fusion_StartCalibration(2000)
```

校准逻辑：

- 先跳过 50 个 1 ms 样本。
- 只在三轴 gyro 绝对值都小于 `3.0 deg/s` 时累计零偏样本。
- 目标样本数 2000。
- 6 s 超时后，只有静止样本数达到 `IMU_MIN_CAL_SAMPLES` 才完成校准；样本不足会设置 `imu_calibration_warning` 并继续等待。
- 校准完成后才执行 Madgwick 姿态更新。

`IMU_Fusion_GetYaw()` 返回：

```c
fmodf(720.0f - yaw, 360.0f)
```

因此上传 yaw 范围是 0..360 度，方向按该转换后的值为准。

---

## 13. 本机菜单与 Flash

IPS200 菜单在 `Menu.c`，参数持久化在 `Flash.c`：

- Flash 存储位置：section `21`，page `4`。
- `Menu_Init()` 启动时调用 `Read_Flash()`；Flash 未初始化或首项非法时保留 `Global.c` 默认值。
- 新写入的 Flash 页带 `TC264_FLASH_MAGIC`、版本号与 payload CRC32；若检测到新格式但 CRC 不匹配，读取会放弃该页并保留默认值。
- 读取后会对关键 PWM、PID、运行时间、无线/蜂鸣器开关和模式枚举做保守限幅，降低坏 Flash 参数直接上车的风险。
- 调参菜单退出时调用 `Write_Flash()`。
- 预设模式在 `Menu_flag.c`：`coin_slow_mode`、`coin_middle_mode`、`coin_fast_mode`、`line_slow_mode`、`line_middle_mode`、`line_fast_mode`。
- `reset_mode()` 当前主要重置车辆元素相关 PWM/PID 参数，并写入 Flash。

重要调参项：

| 菜单项 | 变量 |
|---|---|
| `normal_pid` | `Servo_Kp`, `Servo_Kd` |
| `very_fast_pid` | `S8_PD_Mode`, `Servo_Kp_s8_z/w`, `Servo_Kd_s8_z/w` |
| `closing_car_pid` / `leaving_car_pid` | `Car_PD_Mode`, `Servo_Kp_car_close/leave`, `Servo_Kd_car_close/leave` |
| `element_pwm` | `fast_ps_pwm`, `slow_ps_pwm`, `slow_coin_pwm`, `slow_boundary_pwm`, `slow_coin_side_pwm`, `coin_return_road_pwm`, `very_fast_pwm`, `car_pwm_close`, `car_pwm_leave` |
| `chasu_pid` | `Chasu_D_Mode` 与各模式差速 Kp/Kd |
| `other_data` | `key_Motor_PWM`, `approach_rate`, `runtime`, `buzzer`, `wireless` |
| `Fuzzy_PID` | `Fuzzy_Enable` 与各模糊表 |

---

## 14. 与上位机联调契约

1. 上位机发送固定 9 字节、`len=4`、小端 payload、CRC16-CCITT。
2. `0x01` 或其他有效控制帧必须持续发送；RUNNING 后约 666 ms 未收到 CRC 正确的有效帧会进入 STOP_TEMP。
3. `0x03=1` 是锁存保护，必须 `0x03=0` 才能再次发车。
4. 发车顺序推荐 `0x03=0` → `0x02=0` → `0x05=1`。
5. PRERUN 会等待 IMU 校准完成，并至少经过 `pre_run_time=1` 的秒级节拍。
6. TC264 上传的 `0x01/0x02` 是同一 2 ms 周期快照下的左右轮增量 tick，不是绝对计数或速度。
7. `0x09` 由上位机 DriveState 驱动，但固件当前只对 `3(AvoidCar)` 和 `4(LeavingCar)` 有专门控制分支。
8. `flag_stop==1` 和 UART 超时都会进入 STOP_TEMP；STOP_TEMP 中舵机仍跟 error，电机目标速度为 0。
9. `max_speed > 12` 表示解除速度上限，固件内部上限为 2000 tick/周期量级。
10. 若修改 `send_data.c/.h` 的帧格式、payload 类型或命令字，必须同步上位机 `src/io/uart.cpp` 与 `UartCommander`。

---

## 15. 验证清单

- [ ] 上位机 `0x01` error 到达后，IPS200 或调试状态页能看到 error 变化。
- [ ] RUNNING 中断开 UART 输入约 666 ms 后进入 STOP_TEMP；恢复 `0x01` 且 `flag_stop!=1` 后回 RUNNING。
- [ ] `0x03=1` 后无法 `0x05` 发车；`0x03=0` 后可以。
- [ ] 发车后 PRERUN 会等待 IMU 校准完成；静止样本不足时保持 PRERUN 并提示 `imu keep still`。
- [ ] 编码器上传顺序为 `0x01` 左轮、`0x02` 右轮，且均为增量。
- [ ] 上位机 ODOM 随行驶单调递增，右轮方向与 TC264 取反约定一致。
- [ ] `0x02=1→0` 能让 STOP_TEMP 恢复 RUNNING。
- [ ] `0x09=3/4` 时车辆接近/离开专用 PWM/PID 分支生效；约 2 s 无刷新后清零。
- [ ] `flag_stop==7` 时 `return_track_error_multiplier` 生效，error 被限制在 `[-160, 160]`。
- [ ] `flag_stop==8` 时 `very_fast_pwm` 与 `S8_PD_Mode`/专用模糊表生效。

---

## 16. 常见陷阱

1. **RX/TX 命令字含义相反**：TC264 收到的 `0x01` 是 error；TC264 发出的 `0x01` 是左轮 Δtick。
2. **编码器上传不是绝对计数**：上位机应累加增量，不能把它当总里程。
3. **UART TX 不在 ISR**：ISR 只收字节；上传由主循环 `uart_upload_service()` 处理。
4. **UART 看门狗只认有效帧**：噪声字节、CRC 错帧和未知命令不会清超时。
5. **PRERUN 受 IMU 静止质量影响**：如果车身一直晃动导致静止样本不足，会停留在 PRERUN 并提示 `imu keep still`。
6. **IMU 校准会阻塞 RUNNING**：发车后 RUNNING 必须等校准完成；若长时间不进入 RUNNING，先检查静止条件和 `imu_calibration_warning`。
7. **`flag_stop==0` 不一定普通速度**：若 `flag_element==3/4`，会进入车辆元素速度和 PID 分支。
8. **`0x09` 其他值不等于固件都有动作**：除 3/4 外，多数值当前主要是记录和超时。
9. **`flag_stop==7` 改了舵机输入 error**：它会乘 `return_track_error_multiplier` 后再限幅。
10. **Flash 会覆盖源码默认值**：实车参数以菜单/Flash 当前值为准，源码默认只在 Flash 未初始化时生效。
11. **`wireless` 打印会占用调试输出**：正式跑车建议保持 `wireless=0`。
12. **只改一侧协议风险很高**：TC264、上位机主程序、定位/SLAM 辅助程序中的 UART 语义可能不完全一致；比赛主链以 Xcar 的 `src/io/uart.cpp` 与本文档为准。

---

## 17. 参考文件

| 侧 | 文件 |
|---|---|
| TC264 | `/home/orangepi/Desktop/TC264/user/cpu0_main.c` |
| TC264 | `/home/orangepi/Desktop/TC264/code/send_data.c`, `send_data.h`, `reliability_core.c`, `reliability_core.h` |
| TC264 | `/home/orangepi/Desktop/TC264/code/other_code.c`, `car_run.c`, `pid.c` |
| TC264 | `/home/orangepi/Desktop/TC264/code/encoder.c`, `imu_fusion.c` |
| TC264 | `/home/orangepi/Desktop/TC264/code/Menu.c`, `Menu_flag.c`, `Flash.c` |
| 上位机 | `/home/orangepi/Desktop/Xcar/src/io/uart.cpp`, `/home/orangepi/Desktop/Xcar/src/control/UartCommander.cpp` |
| 上位机 | `/home/orangepi/Desktop/Xcar/include/control/drive_state.h`, `/home/orangepi/Desktop/Xcar/Uart/HardwareProxy.hpp` |

---

*文档版本：2026-08-02。以 `/home/orangepi/Desktop/TC264` 当前源码为准；上位机以 `/home/orangepi/Desktop/Xcar` 当前源码为准。*
